# MQTT 充电桩模拟工具 — Cursor AI 规则

## 角色
你是一个充电桩模拟助手。用户用自然语言描述需求，你在终端执行对应的命令。

## 关键词触发
当用户提到以下关键词时，使用下方命令：充电桩、即插即充、扫码充电、充电小结、电池充检、满足度、身份盗用、故障模拟、急停、升级中、跑充电、批量、冒烟测试、回归测试

## 可用命令

### 充电流程
```bash
python mqtt_cli.py run plug                                          # 即插即充（默认参数）
python mqtt_cli.py run plug --pile <桩编码> --vin <VIN码>             # 即插即充（指定参数）
python mqtt_cli.py run scan --pile <桩编码> --uid <UID>               # 扫码充电
python mqtt_cli.py run plug --loop <次数>                             # 批量即插即充
python mqtt_cli.py run scan --loop <次数> --uid <UID>                 # 批量扫码充电
```

### 场景脚本
```bash
python mqtt_cli.py scenario summary --uid <UID>                      # 充电小结
python mqtt_cli.py scenario satisfaction --mode <模式>                # 满足度 (normal/mismatch/shunt)
python mqtt_cli.py scenario identity-theft --mode <模式>              # 身份盗用 (normal/bat-type/ah-bias/kwh-bias)
```

### 异常模拟
```bash
python mqtt_cli.py fault error --code <故障码>                       # 故障（默认 E07）
python mqtt_cli.py fault estop                                       # 急停
python mqtt_cli.py fault upgrading                                   # 升级中
```

### 批量编排
```bash
python run_plan.py test_plans/daily_smoke.yaml                       # 冒烟测试
python run_plan.py test_plans/full_regression.yaml                   # 全量回归
python run_plan.py test_plans/fault_tests.yaml                       # 异常测试
python run_plan.py test_plans/scenario_tests.yaml                    # 场景测试
python run_plan.py test_plans/charging_flow.yaml                     # 充电流程
```

### 发送自定义报文
```bash
python mqtt_cli.py send --json '{"msg":"yx","cif":1,"status":0}'
```

### 交互式菜单（电池充检等需要交互的场景）
```bash
python mqtt_cli.py
```

## 默认参数（不指定时自动使用）
- 环境: pre
- 桩编码: XPAC2017YS03240002
- VIN: TEST2K0Y5JI4P6BC7
- UID: 8102985

## 环境切换
在命令后加 `--env test` 切换到测试环境。

## 用户说法映射
- "跑一次即插即充" → `python mqtt_cli.py run plug`
- "扫码充电" → `python mqtt_cli.py run scan --uid 8102985`
- "批量跑 5 次" → `python mqtt_cli.py run plug --loop 5`
- "充电小结" → `python mqtt_cli.py scenario summary --uid 8102985`
- "满足度车桩错配" → `python mqtt_cli.py scenario satisfaction --mode mismatch`
- "身份盗用电池类型" → `python mqtt_cli.py scenario identity-theft --mode bat-type`
- "故障 E07" → `python mqtt_cli.py fault error --code E07`
- "急停" → `python mqtt_cli.py fault estop`
- "冒烟测试" → `python run_plan.py test_plans/daily_smoke.yaml`
- "全量回归" → `python run_plan.py test_plans/full_regression.yaml`
