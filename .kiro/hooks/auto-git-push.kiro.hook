{
  "enabled": true,
  "name": "自动推送 Git",
  "description": "每次代理执行完成后，自动将改动提交并推送到 Git 仓库",
  "version": "1",
  "when": {
    "type": "agentStop"
  },
  "then": {
    "type": "runCommand",
    "command": "git add -A && git commit -m \"auto: update from Kiro\" && git push origin main"
  }
}